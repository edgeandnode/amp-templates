# AMP Query Gateway URL Configuration
VITE_AMP_GATEWAY_URL=https://gateway.amp.staging.thegraph.com/

# Optional for local Amp: Access token for AMP Query Gateway authentication
# Run `pnpm amp auth login` and `pnpm amp auth token` and paste `token` value below
# Leave empty if authentication is not required
VITE_AMP_ACCESS_TOKEN=

# Optional: Polling interval in milliseconds for auto-refresh (default: 2000ms / 2 seconds)
VITE_POLLING_INTERVAL=2000

# Optional: Request timeout in milliseconds (default: 30000ms / 30 seconds)
VITE_REQUEST_TIMEOUT=30000
